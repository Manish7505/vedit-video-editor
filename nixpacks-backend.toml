[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["cd server && npm install"]

[phases.build]
cmds = ["echo 'Backend build complete'"]

[start]
cmd = "cd server && npm start"

[variables]
NODE_ENV = "production"
PORT = "8080"

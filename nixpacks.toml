providers = ["node"]

[phases.install]
cmds = ["npm ci --omit=dev --no-audit", "cd server && npm ci --omit=dev --no-audit"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
